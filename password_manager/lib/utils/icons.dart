import 'package:flutter/material.dart';

const EyeOpen = Icon(Icons.remove_red_eye);
const CopyPassword = Icon(Icons.copy_outlined);
const Delete = Icon(Icons.arrow_back_ios);
const Search = Icon(Icons.search);
const Plus = Icon(Icons.add);
const Home = Icon(Icons.home);
const User = Icon(Icons.manage_accounts_sharp);